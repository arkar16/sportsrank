---
status: accepted
---

# Build Releases as immutable full-site overlays

Every candidate Release will begin as a complete overlay of the Published Site and may change only artifacts independently derived as owned by its Ranking Run. Validation will derive the expected graph from the trusted snapshot and run target rather than accepting the manifest's claims, preserving all prior public paths and unchanged legacy artifacts even when a current run generates only part of the site.
