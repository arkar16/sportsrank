---
status: accepted
---

# Publish separate Classification rankings on a comparable scale

Each college-football Classification will receive its own Ranking rather than silently combining teams into one table, while CORS values must retain the same interpretation across those tables. The calibration mechanism is deliberately unresolved until cross-classification behavior is designed and validated; expanding beyond FBS must not invent an incompatible scale by accident.
